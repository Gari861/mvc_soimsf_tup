@model MVC.SOIMSF.Models.AfiliadosViewModel

@{
    ViewData["Title"] = "Detalles del Afiliado";
}

<h1>Detalles del Afiliado</h1>

<div>
    <h4>Afiliado</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Id</dt>
        <dd class="col-sm-10">@Model.Id</dd>

        <dt class="col-sm-2">Nombre</dt>
        <dd class="col-sm-10">@Model.Nombre</dd>

        <dt class="col-sm-2">Apellido</dt>
        <dd class="col-sm-10">@Model.Apellido</dd>

        <dt class="col-sm-2">Cuil</dt>
        <dd class="col-sm-10">@Model.Cuil</dd>

        <dt class="col-sm-2">Teléfono</dt>
        <dd class="col-sm-10">@Model.Telefono</dd>

        <dt class="col-sm-2">Email</dt>
        <dd class="col-sm-10">@Model.Email</dd>

        <dt class="col-sm-2">Fecha de Nacimiento</dt>
        <dd class="col-sm-10">@Model.FechaNacimiento?.ToString("yyyy-MM-dd")</dd>

        <dt class="col-sm-2">Discapacidad</dt>
        <dd class="col-sm-10">@Model.Discapacidad?.ToString()</dd>

        <dt class="col-sm-2">Estado Afiliado</dt>
        <dd class="col-sm-10">@ViewBag.IdEstadoAfiliado</dd>

        <dt class="col-sm-2">Situación de Revista</dt>
        <dd class="col-sm-10">@ViewBag.IdSituacionDeRevista</dd>

        <dt class="col-sm-2">Empresa</dt>
        <dd class="col-sm-10">@ViewBag.IdEmpresa</dd>
    </dl>
</div>

<div>
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">Editar</a>
    <a asp-action="Index" class="btn btn-secondary">Volver a la Lista</a>
</div>
